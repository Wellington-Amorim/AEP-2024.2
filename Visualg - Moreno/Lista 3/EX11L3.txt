algoritmo "nomeIdadeSexoMediaFeminina"
// EX11) Solicite ao usuário o nome, idade e sexo (F/M) de algumas pessoas. A cada entrevista, o sistema deverá perguntar
//se o usuário deseja continuar a informar dados, e caso não queira, apresente a média de idade das mulheres.
// Autor : Wellington Amorim de Sousa
// Data : 31/08/2024
// RA: 24214926-2
var
i, idade, entrevista: inteiro    //i serve para fazer o loop. entrevista é para armazenar a quantidade de entrevistas
nome, sexo: caractere
media: real
inicio
repita
  escreval("Qual o seu nome?")     //nome
   leia(nome)
  escreval("Qual a sua idade? ")       //idade
   leia(idade)
  escreval("Informe o seu sexo(F/M): ")      //sexo
   leia(sexo)
   se(sexo = "F") entao                //se o sexo for F
    media <- media + idade             //adicionar na média
   fimse
  escreval("Deseja comtinuar a entrevista? Se sim, digite 1, senão, digite 2. ")     //opção de parar a entrevista
   leia(i)
  entrevista <- entrevista + 1            //soma as entrevistas
ate(i = 2)               //fim do loop
escreva("A média das idades das mulheres é:",media/entrevista:7:2)       //apresenta a média
fimalgoritmo      //Fim do código.
